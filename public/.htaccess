Options -Indexes
RewriteEngine On

# Quando acessar /catatreco/public/*, mantém o front controller local
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [QSA,L]
